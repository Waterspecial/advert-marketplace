{% extends 'base.html.twig' %}

{% block title %}Browse Adverts{% endblock %}

{% block stylesheets %}
    <style>
        /* Container */

        .advert-list {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 24px;
        }

        /* Header */
        h1 {
            font-size: 2rem;
            font-weight: bold;
            margin-bottom: 32px;
        }

        /* Advert Card */
        .advert-card {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 2px 8px rgba(0,0,0,0.05);
            padding: 24px;
            border: 1px solid #e2e8f0;
            margin-bottom: 32px;
            cursor: pointer;
        }

        .advert-image {
            width: 100%;
            height: 160px;
            border-radius: 12px;
            background: linear-gradient(to right, #e0c3fc, #fbc2eb);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            margin-bottom: 16px;
        }

        .advert-content {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }

        .advert-title {
            font-size: 1.25rem;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .advert-location {
            color: #718096;
            font-size: 0.875rem;
            margin-bottom: 8px;
        }

        .advert-category {
            display: inline-block;
            font-size: 0.75rem;
            color: #5a67d8;
            background-color: #ebf4ff;
            padding: 4px 12px;
            border-radius: 9999px;
        }

        .advert-price {
            font-size: 1.25rem;
            font-weight: bold;
            color: #5a67d8;
            text-align: right;
        }

        .advert-seller {
            font-size: 0.875rem;
            color: #718096;
            margin-top: 8px;
            text-align: right;
        }

        .pagination-container {
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 1000;

            background: #fff;
            padding: 8px 16px;
            border-radius: 8px;
            box-shadow: 0 2px 8px rgba(0,0,0,.1);
        }

        .pagination span {
            display: inline-block;
            padding: 8px 16px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 500;
            background-color: #e2e8f0;
            color: #5a67d8;

        }

        .pagination a {
            text-decoration: none;
        }

        .pagination span.current {
            background-color: #e0c3fc;
            color: #fff;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="">

        <!-- Header -->
        <h1>Browse Adverts</h1>

        <!-- Advert List -->
        <div class="advert-list">
            {% for advert in pagination %}
                <a href="{{ path('app_advert_view', { id: advert.id }) }}" class="advert-card" style="text-decoration: none; color: inherit;">
                    <div class="advert-image">
                        📷
                    </div>

                    <div class="advert-content">
                        <div>
                            <h2 class="advert-title">{{ advert.title }}</h2>
                            <p class="advert-location">📍 {{ advert.location }}</p>
                            <span class="advert-category">{{ advert.category }}</span>
                        </div>

                        <div>
                            <p class="advert-price">£{{ advert.price }}</p>
                            <p class="advert-seller">{{ advert.user.username }}</p>
                        </div>
                    </div>
                </a>
            {% else %}
                <p>No adverts found.</p>
            {% endfor %}
        </div>

        <!-- Pagination -->
        <div class="pagination-container">
            {{ knp_pagination_render(pagination) }}
        </div>


    </div>

{% endblock %}
